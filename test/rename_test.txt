slime slimeCount = 4 + damage / amount
    where
        damage 0      = 0
        damage amount = slimeCount * amount
        amount        = 5

doTest x y = do
    z <- x
    return y

patternTest (MyData is cool) = is cool x
    where
        x = 2

 -- TODO: Clean up type variable scoping!
-- class (Show a) => MyTypeClass a where
--     deathByKilling :: a -> Bool

data DeathType a = DeathType | LifeType a

sigTest :: Eq a => a -> Float
